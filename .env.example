# Copie para .env.local e preencha os valores.
# .env.local não é versionado (gitignore).

# URL base do GeoServer (ex.: https://seu-servidor/geoserver)
VITE_GEOSERVER_URL=

# Usar GeoWebCache para tiles (true/false)
VITE_USE_GEOWEBCACHE=false
